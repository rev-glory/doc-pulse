{
  "name": "docpulse",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "build": "pnpm -r --filter=./apps/* --filter=./packages/* --if-present run build",
    "lint": "pnpm -r --filter=./apps/* --filter=./packages/* --if-present run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,mjs,cjs,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,mjs,cjs,json,md,yml,yaml}\"",
    "test": "pnpm -r --filter=./apps/* --filter=./packages/* --if-present run test",
    "typecheck": "pnpm -r --filter=./apps/* --filter=./packages/* --if-present run typecheck"
  },
  "devDependencies": {
    "prettier": "catalog:",
    "typescript": "catalog:"
  }
}
